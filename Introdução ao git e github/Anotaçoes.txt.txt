git clone URL (clona repo remoto no repo local 

o gitbash é um terminal extendido para utilizar o uso do git 

para clonar o repositório-------------------------------------------------------

git clone URL 

Para atualizar o repositório remoto basta usar os comandos-----------------------

git add. 

git commit -m 'mensagem do commit'

git push origin main
